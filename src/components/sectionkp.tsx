"use client"
import React from 'react';
import RentalOffer from './kp';

const SectionKP = () => {
  return (
    <section className="relative flex justify-center items-center bg-white h-max z-1">
      <RentalOffer />
    </section>
  );
};

export default SectionKP;
